---
import Layout from './Layout.astro';
import { client } from './client';
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<Layout
  title={`À Propos — ${client.nom} | Fast Food Artisanal ${client.ville}`}
  description={`L'histoire de ${client.nom}, fast food artisanal à ${client.ville} près de Perpignan. Découvrez notre équipe et notre passion pour les tacos français et burgers maison.`}
>
  <nav class="nav scrolled">
    <div class="nav-inner">
      <a href={import.meta.env.BASE_URL} class="nav-logo">Street<span>Fusion</span></a>
      <ul class="nav-links">
        <li><a href={import.meta.env.BASE_URL}>Accueil</a></li>
        <li><a href={`${base}/menu`}>Notre Carte</a></li>
        <li><a href={`${base}/a-propos`} class="active">À Propos</a></li>
        <li><a href={`${base}/avis`}>Avis</a></li>
        <li><a href={`${base}#contact`}>Commander</a></li>
      </ul>
      <div class="nav-cta">
        <a href={client.telephoneLink} class="nav-tel">{client.telephone}</a>
        <a href={`${base}#contact`} class="btn btn-primary">Commander</a>
      </div>
      <button class="burger-btn" aria-label="Menu"><span></span><span></span><span></span></button>
    </div>
  </nav>

  <div class="mobile-menu">
    <a href={import.meta.env.BASE_URL}>Accueil</a>
    <a href={`${base}/menu`}>Notre Carte</a>
    <a href={`${base}/a-propos`}>À Propos</a>
    <a href={`${base}/avis`}>Avis</a>
    <div class="mobile-cta">
      <a href={`${base}#contact`} class="btn btn-primary">Commander</a>
    </div>
  </div>

  <!-- HERO À PROPOS -->
  <header class="about-hero">
    <div class="container">
      <div class="breadcrumb reveal">
        <a href={import.meta.env.BASE_URL}>Accueil</a><span>→</span><span>À Propos</span>
      </div>
      <h1 class="page-title reveal reveal-d1">
        Notre <span class="flame-text">Histoire</span>
      </h1>
    </div>
  </header>

  <!-- STORY GRID -->
  <section class="section">
    <div class="container">
      <div class="story-grid">
        <div class="story-content reveal slide-left">
          <p class="section-label">{client.ville}, {client.codePostal}</p>
          <h2 class="section-title">{client.apropos.titre}</h2>
          <p class="story-sub">{client.apropos.sousTitre}</p>
          <p class="story-text">{client.apropos.paragraphe1}</p>
          <p class="story-text">{client.apropos.paragraphe2}</p>
          <div class="story-cta">
            <a href={`${base}/menu`} class="btn btn-primary">Voir notre carte</a>
            <a href={`${base}#contact`} class="btn btn-outline">Commander</a>
          </div>
        </div>
        <div class="story-img reveal slide-right">
          <div class="img-frame">
            <img src={client.apropos.image} alt={client.apropos.imageAlt} loading="lazy" />
            <div class="img-badge">
              <strong>Depuis 2022</strong>
              <span>Le Soler, 66</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- VALEURS -->
  <section class="section-alt">
    <div class="container">
      <div class="section-header reveal" style="text-align: center; max-width: 500px; margin: 0 auto 48px;">
        <p class="section-label">Ce qui nous anime</p>
        <h2 class="section-title">Nos <span>Valeurs</span></h2>
      </div>
      <div class="valeurs-grid">
        {client.apropos.valeurs.map((v, i) => (
          <div class={`valeur-card reveal reveal-d${i + 1}`}>
            <div class="valeur-icon">{v.icone}</div>
            <h3 class="valeur-title">{v.titre}</h3>
            <p class="valeur-desc">{v.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section" style="text-align: center;">
    <div class="container">
      <h2 class="section-title reveal" style="margin-bottom: 20px;">
        Venez nous <span>rendre visite</span>
      </h2>
      <p class="reveal reveal-d1" style="color: var(--text-muted); margin-bottom: 32px;">
        {client.adresse} — {client.ville} ({client.codePostal})
      </p>
      <div class="reveal reveal-d2" style="display: flex; gap: 16px; justify-content: center; flex-wrap: wrap;">
        <a href={`${base}#contact`} class="btn btn-primary btn-lg">Commander maintenant</a>
        <a href={client.telephoneLink} class="btn btn-outline btn-lg">📞 {client.telephone}</a>
      </div>
    </div>
  </section>

  <footer class="footer">
    <div class="container">
      <div class="footer-bottom" style="border-top: none; padding-top: 0;">
        <p>© {new Date().getFullYear()} {client.nom} — {client.ville}. <a href={`${base}/mentions-legales`} style="color: var(--text-muted);">Mentions légales</a></p>
        <a href="https://lorn.dev" target="_blank" rel="noopener" class="lorn-badge">Site par LORN Digital Studio</a>
      </div>
    </div>
  </footer>
</Layout>

<style>
  .nav.scrolled {
    background: rgba(10,10,10,0.97);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
  }

  .about-hero {
    padding: 120px 0 60px;
    background: var(--bg-alt);
    border-bottom: 1px solid var(--border);
  }

  .breadcrumb {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    color: var(--text-muted);
    margin-bottom: 20px;
  }

  .breadcrumb a:hover { color: var(--primary); }

  .page-title {
    font-family: var(--font-display);
    font-size: clamp(3rem, 7vw, 6rem);
    text-transform: uppercase;
    letter-spacing: -0.03em;
    line-height: 0.9;
  }

  .story-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  .story-sub {
    font-size: 1.1rem;
    font-style: italic;
    color: var(--primary);
    margin: 16px 0 24px;
    line-height: 1.6;
  }

  .story-text {
    font-size: 1rem;
    color: var(--text-muted);
    line-height: 1.8;
    margin-bottom: 16px;
  }

  .story-cta {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    margin-top: 32px;
  }

  .img-frame {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
  }

  .img-frame img {
    width: 100%;
    aspect-ratio: 4/5;
    object-fit: cover;
  }

  .img-badge {
    position: absolute;
    bottom: 20px;
    right: 20px;
    background: var(--primary);
    border-radius: var(--radius-md);
    padding: 12px 18px;
    text-align: center;
  }

  .img-badge strong {
    display: block;
    font-family: var(--font-display);
    font-size: 1.2rem;
    color: white;
  }

  .img-badge span {
    font-size: 12px;
    color: rgba(255,255,255,0.8);
  }

  .valeurs-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }

  .valeur-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    padding: 28px 24px;
    text-align: center;
    transition: border-color 0.2s, transform 0.2s;
  }

  .valeur-card:hover {
    border-color: var(--primary);
    transform: translateY(-4px);
  }

  .valeur-icon {
    font-size: 2.5rem;
    margin-bottom: 16px;
  }

  .valeur-title {
    font-family: var(--font-display);
    font-size: 1.1rem;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    margin-bottom: 10px;
    color: var(--text);
  }

  .valeur-desc {
    font-size: 13px;
    color: var(--text-muted);
    line-height: 1.6;
  }

  @media (max-width: 1024px) {
    .valeurs-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 768px) {
    .story-grid { grid-template-columns: 1fr; gap: 40px; }
    .valeurs-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 480px) {
    .valeurs-grid { grid-template-columns: 1fr; }
  }
</style>
