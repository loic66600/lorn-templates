---
// ══════════════════════════════════════════════════════════════
// PAGE — ZONE D'INTERVENTION
// DÉSACTIVABLE : mv src/pages/zone.astro src/pages-off/zone.astro
// ══════════════════════════════════════════════════════════════
import Layout from '../layouts/Layout.astro';
import { client } from '../data/client';
const communes = client.zones.split(", ");
---
<Layout title={`Zone d'intervention — ${client.nom}`}>
  <div class="page-top"><div class="container" style="display:flex;justify-content:space-between;align-items:center;"><a href="/" class="back-link">← Retour</a><span style="font-weight:700;color:var(--blue-light);font-size:15px;">{client.nom}</span></div></div>
  <section class="section"><div class="container">
    <div style="text-align:center;margin-bottom:48px;">
      <span class="sec-tag reveal font-mono">// ZONE D'INTERVENTION</span>
      <h1 class="sec-title reveal reveal-d1">Nous intervenons dans un rayon de <span class="text-blue">{client.rayonKm}</span></h1>
      <p class="sub reveal reveal-d2">Dépannage, installation, mise aux normes — nous nous déplaçons dans tout le département.</p>
    </div>

    {client.googleMapsEmbed && (
      <div class="zone-map reveal" style="margin-bottom:40px;">
        <iframe src={client.googleMapsEmbed} width="100%" height="400" style="border:0;border-radius:16px;filter:invert(0.92) hue-rotate(180deg) brightness(1.1) contrast(0.9);" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
      </div>
    )}

    <h3 class="zone-subtitle reveal">Communes desservies</h3>
    <div class="zone-grid reveal reveal-d1">
      {communes.map(c => <span class="zone-badge font-mono">{c}</span>)}
    </div>

    <div class="zone-note reveal" style="margin-top:32px;">
      <p>📍 Votre commune n'apparaît pas ? Contactez-nous, nous intervenons peut-être chez vous.</p>
    </div>

    <div style="text-align:center;margin-top:48px;" class="reveal">
      <a href="/#devis" class="btn-blue btn-lg">Demander un devis gratuit →</a>
    </div>
  </div></section>
</Layout>
<style>
  .page-top{padding:100px 0 0;} .back-link{color:var(--blue-light);font-size:14px;font-weight:500;}
  .sec-tag{display:block;font-size:11px;color:var(--blue);letter-spacing:0.15em;margin-bottom:8px;}
  .sec-title{font-size:clamp(1.6rem,3.5vw,2.4rem);font-weight:700;color:var(--white);line-height:1.15;}
  .sub{font-size:15px;color:var(--text-muted);max-width:480px;margin:10px auto 0;}
  .zone-subtitle{font-size:18px;font-weight:700;color:var(--white);margin-bottom:16px;}
  .zone-grid{display:flex;flex-wrap:wrap;gap:8px;}
  .zone-badge{padding:8px 16px;border-radius:8px;background:var(--bg-card);border:1px solid var(--border);font-size:13px;color:var(--text);transition:all 0.3s;}
  .zone-badge:hover{border-color:var(--blue);color:var(--blue-light);}
  .zone-note{padding:16px 20px;border-radius:12px;background:var(--blue-glow);border:1px solid var(--border-blue);}
  .zone-note p{font-size:14px;color:var(--text);line-height:1.6;}
  .btn-blue{display:inline-flex;align-items:center;gap:8px;padding:15px 32px;border-radius:12px;background:var(--blue);color:#fff;font-weight:700;font-size:15px;border:none;cursor:pointer;transition:all 0.3s;} .btn-blue:hover{background:var(--blue-dark);transform:translateY(-2px);} .btn-lg{padding:18px 40px;font-size:16px;}
</style>
